import { ReactElement } from "react";
import PlanTrip from "./PlanTrip";

export default {
  title: "Components/Plan-Trip",
  component: PlanTrip,
};

export const PlanTripComponent = (): ReactElement => {
  return <PlanTrip />;
};
